spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  port: 8081

eureka:
  client:
    registryFetchIntervalSeconds: 5
    
ribbon:
  IsSecure: false

fortunes:
  ribbon:
    ServerListRefreshInterval: 5000

security:
  basic:
    enabled: false

management:
  security:
    enabled: false
